# Core data processing (pandas>=2.2 recommended if Colab/joblib loads analysis_bundle.joblib from this repo)
pandas>=2.2.0
# Keep numpy major the same where you train and where you joblib.load the bundle (Colab vs local).
# If loads fail with ValueError from numpy: pip install "numpy<2" OR re-train on matching numpy 2.x.
numpy>=1.24.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
tensorflow>=2.15.0
statsmodels>=0.14.0
arch>=6.3.0
pyarrow>=15.0.0

# Data visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# yahoo stock data
yfinance>=0.2.0

# Web dashboard (python web_app.py or start_web.py)
Flask>=2.3.0
Werkzeug>=2.3.0

